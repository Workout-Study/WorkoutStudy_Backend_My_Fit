package com.fitmate.myfit.adapter.`in`.web.common

class GlobalURI {

    companion object {
        const val ROOT_URI = "/fit-group-service"

        const val FIT_RECORD_ROOT = "$ROOT_URI/groups"
    }
}